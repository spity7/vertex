# Used by docker-compose build args, npm run dev (.env.local), and runtime email
# Copy to .env for Docker, or .env.local for: npm run dev

NEXT_PUBLIC_API_URL=https://api.vertex-engineering.co/api/v1
EMAIL_USER=
EMAIL_PASS=
